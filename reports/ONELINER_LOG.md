# 一句话重写日志

> 2026-09-06，Codex GPT-6 在不引入正文外事实的约束下重写 51 篇解读的「一句话」。格式：目录 | 旧 → 新。

1308.0215_leonard_survey | 这篇综述是 SB 的数学地基：它把端点匹配、路径相对熵与最优传输放进同一框架，划清了「熵正则运输」何时成立、何时不能直接套用的边界。 → 以端点熵投影保留参考桥，这篇综述给后续神经桥研究划下数学准绳：连无限质量参考的解结构都能纳入，却明确最优传输代价取决于参考动力学，画出噪声到数据的轨迹远不足以自称薛定谔桥。
1707.06347_ppo | PPO 用概率比裁剪抑制同批数据反复优化时的过大策略更新，以一阶优化换取较好的样本效率和训练稳定性，让策略梯度更容易实现和复用。 → 概率比裁剪让同批数据支持多轮一阶更新，PPO把限制更新落成实用目标：七项控制消融中裁剪版汇总分为0.82、无裁剪无惩罚为−0.39，但用于生成策略时，逐步裁剪仍管不住整条路径的密度比。
1801.01290_sac | SAC 把最大熵策略改进写成可直接优化的随机 actor 损失，让经验回放与熵驱动探索同时工作，推进了复杂连续控制中样本效率与训练稳定性的折中。 → 显式随机策略与最大熵递推把回放数据接入策略改进闭环，SAC在二十一维动作的人形等困难仿真控制上展现更强学习表现，给生成式控制立下奖励与熵的实证基准，换成桥策略仍须证明收益抵得过采样成本。
1910.00177_awr | AWR 把经验回放中的策略改进变成优势加权的监督回归，使同一套价值拟合与策略拟合程序既能在线学习，也能直接利用带奖励的静态数据。 → AWR用优势权重重拟合回放动作，把价值基线与监督更新拼成实用策略搜索：每任务百万步静态数据上能接近或超过示范策略，其启示是已有动作的筛选本身足以改进策略，生成模型的收益必须与奖励筛选的收益分开。
2005.09814_mdpo | MDPO 用多步梯度优化显式 KL 邻近目标，在连续控制中证明不做 TRPO 式硬约束线搜索也能取得强表现，并把“靠近旧策略”与 SAC 的熵正则区分开来。 → 多步梯度让显式KL邻近惩罚真正参与更新，MDPO把镜像下降从表格理论带入深度控制：离策略版与SAC总体相当，也给桥策略提供了优势收益加路径邻近惩罚的推广接口，算法是否等价必须查参考测度与目标。
2006.11239_ddpm | DDPM 把扩散模型训练改造成噪声预测回归，在无条件图像生成上拿到可与强 GAN 竞争的质量，使“逐步去噪”成为有实证支撑的生成路线。 → 噪声预测与重加权训练联手，让DDPM把扩散和分数匹配接成可复用的生成配方：同一固定方差模型的CIFAR-10 FID从13.51降至3.17，奠基价值在参数化与权重的合力，不能把成功简化成预测目标的单点创新。
2011.13456_score_sde | Score-SDE 用连续时间扩散统一 DDPM 与多尺度 score 模型，并从同一个 score 导出随机采样器和概率流 ODE，使生成模型的训练与采样算法能够分开设计。 → 连续时间扩散把去噪模型与多尺度分数模型统一到可换采样器的框架：CIFAR-10的FID达2.20，其分量是打开噪声路径和似然计算的设计空间，同边缘的随机过程与确定流仍不能被当成同一条桥。
2106.01345_decision_transformer | DT 用目标回报和历史轨迹驱动 Transformer 预测动作，让不做 Bellman 更新的监督学习也能与强离线 RL 基线竞争，打破了策略改进必须依赖值函数或策略梯度的惯性。 → DT以目标回报和长历史预测动作，把离线控制推向可复用的序列建模：Key-to-Door成功率94.6%，却与只克隆成功轨迹相当，站稳的是回报条件化的竞争力，长程注意力仍没有独占这份成功的证据。
2106.01357_dsb | DSB 把路径空间的 IPF 投影改写成前后向扩散网络的交替回归，让生成模型在较短扩散时间下反复修正两端分布，而不必只靠把前向加噪拖长来接近高斯先验。 → 前后向扩散核交替回归，让DSB把熵投影与分数生成接成可执行的高维算法：MNIST十二步采样从首轮到第八轮改善，增量是反复校正双端并容纳非高斯目标，端点拟合好仍不足以证明耦合最优。
2205.09991_diffuser | Diffuser 联合去噪整段状态—动作轨迹，把规划改造成可用奖励和目标约束操纵的采样过程，奠定了扩散规划路线的核心接口：换任务可以换引导，而不必重训轨迹模型。 → 联合去噪整段状态与动作，再以目标引导采样，Diffuser把扩散接成可复用的轨迹规划器：指定次序堆叠得分45.6、BCQ与CQL均为零，模型复用的筹码是采样接口，生成计划与直接生成动作必须分开评判。
2208.06193_diffusion_ql | Diffusion-QL 把扩散行为克隆与穿过采样链的 Q 梯度联合训练，证明多模态策略可以直接参与离线策略改进，而不必退回单高斯动作分布。 → 扩散行为克隆与穿过采样链的Q梯度联训，让多步隐式策略同时承担拟合与改进：Gym平均分88.0对CQL的77.6，给桥策略立下的门槛已是无需动作似然的多模态价值优化，新增耦合必须拿出同预算收益。
2209.03003_rectified_flow | Rectified Flow 用速度回归和自生成端点重训把流拉向恒速直线，将少步生成变成可学习的路径几何问题，但拉直并不等于求出最优传输。 → 速度回归与自生成端点重训把少步生成变成耦合几何问题：重流后一步Euler的FID为12.21，额外蒸馏才降至4.85，拉直、改耦合与蒸馏必须分别核算，运输代价下降也不能被桥策略冒领为回报提升。
2210.02747_flow_matching | Flow Matching 把连续正规化流训练改成无需解 ODE 的条件速度回归，让概率路径成为可设计对象，而条件 OT 路径证明了摆脱扩散时间表能同时改善训练与采样。 → 条件速度回归免去训练仿真，让FM打开扩散之外的路径设计：同架构ImageNet 64×64实验平均调用数由DDPM的264降到138，奠基价值在无偏训练速度场，条件路径的最优性仍不能替全局耦合背书。
2302.00482_ot_cfm | OT-CFM 把端点配对从随机抽签改成小批量最优传输，让条件速度回归更接近低代价传输，真正新增的设计自由度是耦合，而不是又一种去噪损失。 → 小批量最优传输重排端点配对，在 CIFAR-10 上仅添不足百分之一的训练开销，把流匹配的设计重心推向耦合几何，也划清了边界：批内配对更优，不等于总体运输最优或终点拟合处处领先。
2302.05872_i2sb | I2SB 用成对图像的解析桥，把修复起点从纯噪声改为退化图像，避开通用 SB 的交替求解，并以少步修复结果证明：生成起点所带的结构信息值得直接利用。 → 以成对图像的解析条件桥从退化图出发，在二至三成自由形状遮挡的补全中用二至十次评估接近最佳表现，而对照至少需百次，以放弃无配对适用性为代价，把结构起点的价值与特殊边界下可扩展的桥训练一起做实。
2303.04137_diffusion_policy | Diffusion Policy 用观测条件扩散联合生成动作块，再滚动执行，把机器人模仿学习从“选一个动作”推进到“选择并保持一种行为模式”，确立了可直接上真机的动作生成接口。 → 观测条件扩散联合生成动作块并滚动执行，以真机 Push-T 最佳配置 95% 的成功率，把扩散策略的分量坐实在机器人系统接口上：多模态表达接上视觉与闭环执行，才是后续生成器必须继承的基座。
2303.16852_dsbm | DSBM 把参考桥重建与双向漂移回归组成可迭代求解器，显著缓解 DSB 的误差积累，使「学到两端分布」进一步接近「学对两端之间的 SB 耦合」。 → 参考桥重建与双向漂移回归交替修正耦合，在五十维高斯实验中比 DSB 和单向迭代更稳定，把一次匹配推进到持续改进端点关系，也为策略蒸馏立下更硬的检验：终点拟合再好，都不能冒充策略改进。
2307.03672_sf2m | SF2M 用静态端点耦合和解析布朗桥同时训练概率流与 score，把随机桥学习移出反复模拟模型的外循环，代价是把 SB 的准确性押在端点耦合的近似质量上。 → 静态耦合配合解析布朗桥同时回归速度与 score，五十维高斯终点相对熵仅 0.124，却未赢下跨时刻平均误差，揭穿了终点更准就等于桥更准的捷径：免模拟求桥省掉动态迭代，准确性仍取决于静态耦合的近似质量。
2310.02233_gsbm | GSBM 把带状态费用的分布运输拆成条件控制与漂移匹配，让路径优化在训练中保持接近目标边际，推进了 SB 的数值求解而非另造一个 GSB 问题。 → 把带状态费用的条件控制嵌入漂移匹配，以千维意见去极化实验将中间路径的优化推进到高维分布运输，真正占住的是给定费用下的求解能力：路径怎么走终于进入可训练算法，把奖励填进去却还称不上通用强化学习。
2402.03300_grpo | GRPO 用同题多回答的相对奖励替代学习型价值基线，省去大模型 PPO 中整套 critic 训练，并在强数学 SFT 模型上验证了继续做策略优化的收益。 → 同题回答的组内相对奖励替代价值基线，MATH 无工具正确率从 46.8% 升至 51.7%，改写的是裁剪策略更新的优势来源：采样分摊了基线估计，强化学习收益却不能直接记成组基线胜过价值网络。
2403.01717_soft_sb | SSB 用终端 KL 惩罚替代精确匹配，证明软约束会改变最优目标分布，从而为借助参考数据抑制小样本噪声提供控制论依据，但简单的“目标与无控终端几何混合”只在固定初态下成立。 → 终端相对熵惩罚松开精确匹配，仅五十张带噪目标图像的实验就暴露过强匹配的代价，也给策略更新划出数学边界：路径正则能否化成显式终端更新取决于初态，形式相似的几何混合不能充当等价证明。
2405.07503_consistency_policy | Consistency Policy 把扩散策略蒸馏为一步或三步动作生成器，以真机约九倍的端到端降延迟证明了视觉策略可以用训练成本换部署速度，但没有消除多模态与长程精度的代价。 → 局部一致性蒸馏把扩散教师压成一步动作生成器，在真机垃圾清理中将推理延迟从一百九十二压至二十一毫秒且成功率持平，立起已有教师的部署压缩基线，但比较后续一步策略时，教师预训练与学生蒸馏都必须算账。
2405.14449_adv_sbm | ASBM 用离散时间 IMF 与条件对抗生成器直接学习少步 SB 转移，在人脸翻译中以 4 次网络评估胜过 100 次评估的 DSBM，把少步化放进求解器，而非留给事后蒸馏。 → 离散交替投影配合条件对抗生成器直接学少步桥转移，在人脸翻译中以四次网络评估取得比百次评估的 DSBM 更低的 FID，把加速推进到求解器内部：值得继承的是同一最优端点耦合下的离散求桥，对抗训练只是实现选择。
2409.00588_dppo | DPPO 把动作去噪嵌入环境交互的两层 MDP，以可计算的逐步高斯似然执行 PPO，并用长程操作实验证明：预训练充分时，长去噪链并不必然拖垮策略梯度微调。 → 把去噪链接入环境交互的两层决策过程，用逐步高斯似然做裁剪更新，真机单腿装配的二十次仿真迁移试验成功十六次，坐实多步生成策略奖励微调的强基线地位，也让桥方法必须回答路径约束究竟比逐步更新多带来什么。
2409.09347_sb_flow | SB Flow 把 IMF 的整轮重训改成有测度收敛依据的在线微调，让 SB 求解摆脱反复训练与缓存管理的负担，但省下的是训练组织成本，采样成本仍在。 → 双向在线漂移回归把整轮桥重训拆成持续小更新，在五十维高斯实验中更快逼近目标端点协方差，将耦合修正从外层重训推进到在线微调，价值落在训练组织的简化：它仍保留布朗参考和熵正则目标，采样成本也仍在。
2502.02538_fql | FQL 让流模型负责行为拟合、一步学生负责 Q 优化，用同噪声蒸馏连接两者，把多模态离线策略改进从整条生成链的反传中解放出来。 → 同噪声动作蒸馏把流教师与一步价值优化学生接合，实测训练快于穿流反传、推理快于重采样选优，把生成策略改进从整条链的时间反传中解放出来，也为桥教师留下清晰入口：能继承端点映射，不能自动继承路径分布。
2505.05470_flow_grpo | Flow-GRPO 用带漂移修正的随机采样和少步训练，把 GRPO 接上预训练流匹配模型，证明无需 critic 也能显著纠正文生图的组合错误，但这还不是机器人策略优化的证据。 → Flow-GRPO 以漂移修正的随机链接上组内优势，将流模型在线策略梯度做成可训练方案，组合生成总分从0.63升至0.95，但这一早期实现的证据止于文生图，尚未跨过机器人双层决策的门槛。
2505.13447_meanflow | MeanFlow 把学习对象从瞬时速度换成区间平均速度，让生成器无需教师、蒸馏或课程训练就能直接学习一步输运，并把 ImageNet 256×256 的一步扩散／流模型质量推进到 1-NFE FID 3.43。 → MeanFlow 用平均速度恒等式学习有限区间位移，在 ImageNet 上做到一步 FID 3.43，把生成质量的突破口移向学习对象本身，轨迹无需预先拉直，双时间变量也不能再冒充它独有的原创点。
2505.22094_reinflow | ReinFlow 在流策略的动作生成链中学习有界噪声，用精确路径概率接上 PPO，让少步乃至一步策略也能从在线交互中改进，补上了流策略快推理却难微调的关键缺口。 → ReinFlow 用可学噪声与精确路径概率接上 PPO，将 Can 成功率从57.83%推至98.50%，补齐扩散在线微调的流模型对位方案，让少步生成有了奖励驱动的改进接口，但真机与大型策略的收益仍无证据。
2506.15799_dsrl | DSRL 冻结扩散或流策略，只用 RL 学习输入噪声，就能在真机上改进预训练行为，把生成式策略优化的接口从大模型权重移到了采样起点。 → DSRL 冻结生成器、只训练输入噪声，把策略优化的战场从模型权重移到源分布，方块入碗经约四十回合在线训练，十次测试的成功数从二升至九，说明噪声控制已是可用的真机适配接口，覆盖能力仍受基础策略限制。
2507.10543_mp1 | MP1 把 MeanFlow 的平均速度学习用于点云条件机器人策略，无须教师蒸馏就以一次前向生成动作块并超过所测扩散基线，让“少步采样”不再足以单独支撑新策略的贡献。 → MP1 用平均速度学习和特征分散正则一次生成动作块，在每任务十条示范下将平均成功率做到78.9%，把一步机器人策略变成 SB 必须正面应对的实测基线，继续拿少步采样本身当贡献已不够。
2511.14759_recap | RECAP 用价值函数生成“好动作”条件标签，把示范、自主经验和人工纠正纳入同一套 VLA 训练，在困难叠衣与浓缩咖啡任务上使成功吞吐翻倍，给流匹配策略提供了绕开连续动作精确似然的实用 RL 配方。 → RECAP 把二值优势变成动作条件，让示范、自主经验与人工纠正在同一监督流程中转化为策略改进，困难叠衣与浓缩咖啡的成功吞吐翻倍有余，其分量在于打通可部署的大规模 VLA 学习系统。
2512.07212_bridge_policy | BridgePolicy 把观测编码成扩散桥的采样起点，在仿真与真机操纵中证明“边界也可承载条件”具有实用价值，但这份证据支持的是十步模仿策略，尚未兑现少步生成或在线强化学习。 → BridgePolicy 将观测表示直接设为桥端点，以配对对齐打通异构观测与动作，五项真机任务平均成功率达90%，把有信息边界做成可用的操纵设计，但十步成绩尚不能替少步控制背书。
2512.19347_omp | OMP 用独立方向监督纠正 MeanFlow 对低模长目标的训练偏向，在一步推理下超过 MP1，并提供以部分成功率换取训练显存的有限差分版本。 → OMP 用端点方向监督纠正平均速度回归的模长偏向，将仿真平均成功率从 MP1 的78.9%推至82.3%，把一步策略的竞争进一步推向训练信号质量，采样器已压到一步，方向仍有纠正余地。
2601.20701_dmpo | DMPO 把 MeanFlow、表征分散正则与 PPO 接成可在线改进的一步视觉策略，证明一步部署不必停在模仿学习，但“彻底打破效率与性能权衡”的结论超出了实验。 → DMPO 用平均速度头、表征分散正则和路径概率 PPO 打通一步视觉策略的在线改进，真机端到端延迟为9.6毫秒，把竞争推进到一步策略能否接受奖励优化，但主文与附录成绩冲突仍卡住稳定性判断。
2602.12829_flac | FLAC 用生成路径的动能替代显式熵计算，把广义 Schrödinger Bridge 正则做成了可用的在线 actor-critic，但确定性流拿到的是几何约束，不能据此宣称严格复现 SAC 的最大熵目标。 → FLAC 把生成路径动能接入在线策略与价值更新，训练与评估每次生成仅用两次网络求值，已占住动能正则生成式策略这条路线；确定性版本只约束几何距离，后来者再用动能替代熵不足以构成方法贡献。
2602.13810_mvp | MVP 用瞬时速度约束稳住一步平均速度策略，再靠 Q 值筛选候选动作做 offline-to-online RL，其价值是接近强多步基线的成功率下显著加速训练，而非大幅刷新成功率。 → MVP 用瞬时速度约束与 Q 值筛选稳住一步控制，平均在线训练每秒153.6次迭代，成功率则接近最强多步基线，把竞争焦点落在省去教师的训练结构与可靠的价值选择，生成器更快须兑现为更好的控制。
2602.23737_bdg | BDGxRL 用 DSB 把可交互源域的转移改写成目标域风格，再重估奖励供 SAC 学习，将无奖励目标示范从模仿素材变成了修正训练动力学的依据。 → BDGxRL 用扩散桥翻译转移并重估奖励，在十八个跨域设置中均取得最高平均分，把 SB 与强化学习的接口移到训练样本，跨域翻译这条路已有先例，后续贡献必须回答是否真正修正了条件动力学。
2603.01469_mf_vla | 这篇工作把 MeanFlow 接入 VLA 动作头，在真机上大幅缩短动作生成时间，却付出明显的堆叠精度代价，价值在于揭示一步 VLA 的实际权衡，而非证明无损替换。 → 一步 VLA 用平均速度头直接生成动作块，在真机上较 SmolVLA 丢掉17.5个百分点的堆叠分数，把一步部署的精度代价摆上台面；可继承的是训练消融与集成经验，提速本身不能充当策略改进。
2603.12480_ofp | OFP 用无需独立预训练教师和 JVP 的自蒸馏，加上条件自引导与动作块复用，让一步模仿策略在所测任务的平均成功率上超过多步基线，把一步生成从速度折衷推进为可竞争的策略训练方式。 → 无需独立教师的自蒸馏将一步模仿提升为多步策略的正面对手：七个二维任务平均成功率68.3%，已超过百步流策略的67.2%，一步强化学习的理论与实验缺口却仍未填补。
2603.18992_sb_foundations | 这份教程把静态运输、路径控制与生成算法接成一张可查的数学路线图，是研究者理解 SB 方法关系、定位各算法假设的教学索引。 → 把端点耦合、路径控制与测度投影逐项对齐，这份教程划清了“生成模型都在解同一座桥”的概念边界：证据来自定理与推导而非统一性能排名，能查清理论出处，却不能替机器人策略选出求解器。
2603.21621_gsb_mdpo | GSB-MDPO 把 MDPO 的邻近项搬到生成路径，用可计算的路径 KL 控制终端动作分布漂移，为多步生成策略提供了不依赖终端动作似然的 on-policy 更新目标。 → 把镜像下降的邻近约束搬到整条生成路径，已占住用路径KL更新生成策略的理论位置：HalfCheetah四至三十二步的消融最终性能相近，实用证据已到仿真，视觉操纵与真机的缺口仍未填上。
2604.03540_dbpo | DBPO 用漂移场回归训练一步动作生成器，再用可精确计算条件似然的高斯接口接入 PPO，说明一步策略不必依赖多步教师或平均速度恒等式，也能从模仿学习走向在线优化。 → 漂移回归把分布修正搬到训练期，固定潜变量的高斯似然再打通在线优化接口，关键分野转向“怎样获得似然”：模仿策略真机仅45/60成功，说明一步部署已落地，真机在线强化学习仍未获证。
2604.05673_rsbm | RSBM 用条件先验缩短轨迹生成距离、用桥核方差缩放降低回归目标的随机性，在室内导航仿真中以 3 步积分取得 92% 成功率，给少步桥式策略提供了闭环证据。 → 学习先验与桥核方差缩放联手，把少步桥式策略推到闭环验证：室内导航仿真三步成功率92%，足以成为具身桥策略的对照锚点，却尚不能把监督导航的成绩算作奖励优化的胜利。
2604.14698_mfpo | MFPO 用平均散度网络近似动作似然、用有效样本量加权的速度估计训练两步 MeanFlow 策略，把最大熵 RL 的主要采样成本压下来，但保留下来的似然偏差仍是其理论与实践之间的缺口。 → 平均散度近似与有效样本量加权的速度回归，让少步生成保住显式熵目标：两步单样本推理0.46毫秒，压低了最大熵学习的采样成本，但受限策略类下的回归仍不等于反向KL投影。
2605.12416_fmq | FMQ 把一步流策略的在线学习目标改成信赖域内的归一化 Q 梯度位移，在论文基准上超越 MVP，说明生成器可以直接学习局部改进方向，而不必靠候选筛选来构造训练目标。 → 把归一化价值梯度的信赖域位移直接变成一步策略的学习目标，已在统一协议下超越MVP：成功率四分位间均值由0.75升至0.91，真正改写的是在线更新规则，推理仍靠多候选筛选。
2606.01151_lp_ds | LP-DS 用受拉格朗日约束的状态与噪声条件残差改进冻结生成策略，把潜变量 RL 的多样性损失变成可调、可测的权衡，但小扰动既不是精确 KL 信赖域，也不保证模式永不坍缩。 → 用受预算约束的噪声残差修正冻结策略，把潜变量适配从替换先验改成受控偏移：真机抓放由18/40升至33/40，但这来自仿真适配后的部署，保留多样性的价值也必须靠模式覆盖而非平均回报验明。
2606.14255_reactvla | ReactVLA 把已有 iMF 动作生成与注意力残差组合成 0.39B 的 VLA，在 LIBERO 用两步生成达到 88.0% 成功率和 18.3 ms 延迟，说明轻量策略可以靠动作头与特征传递设计改善速度和成功率的折中。 → 平均速度动作头配合跨层注意力残差，让轻量视觉语言动作模型拿出速度与成功率兼顾的基线：仿真两步生成在LIBERO达88.0%，真机却需五步，成绩支撑少步工程组合，还不足以宣布一步取代多步。
2606.19656_df_expense | DF-ExpEnse 用生成策略提出候选，再按保守价值与 critic 分歧选择在线探索动作，证明采集规则可以改善既有微调算法的样本效率，而最终评估无需保留这套多候选开销。 → 用保守价值与网络分歧筛选默认三个候选，生成先验由动作模型变成在线探索的过滤器：关闭选择模块后的最终评估仍显示多任务收益，贡献落在数据采集接口，尚不能算作免似然强化学习的新理论路线。
2608.06893_prism | PRISM 把高斯复原桥的参考噪声与采样调度变成可计算的设计问题，但真实图像上的排序反转表明，它给出的是有边界的理论基准，尚不能取代参考过程的实测选择。 → 按后验残差谱设计参考噪声，把桥的参考选择变成可计算问题：FFHQ用五十次函数评估时白噪声FID为7.87、匹配参考却为8.45，迫使策略研究同时控制求解预算、调度与学习误差，高斯最优公式不能直接照搬。
2608.16153_uca_flow | UCA-Flow 让观测、时间和动作 token 在同一 Transformer 内逐层双向更新，并用平均与瞬时速度双重监督改善一步生成，证明条件如何进入网络也是少步策略的关键设计变量。 → 观测与动作逐层双向更新，配合瞬时速度监督，把一步策略的竞争推向条件建模：货架放置任务改用交叉注意力后成功率从74.0%降至56.6%，桥策略若只靠更充分利用观测取胜，就必须先过这道对照。
