你是一名研究综述编辑。仓库 awesome_study 下 `papers/*/README.md` 共 51 篇论文解读，每篇第一节是「## 一句话」。上一轮事实核对后，很多「一句话」变得准确但平淡（像摘要句），失去了判断力。你的任务是**只重写「一句话」这一节**，让它重新锋利，同时不引入任何该 README 正文里没有的事实。

## 对每篇的要求
1. 读整篇 README（正文是你唯一的事实来源；不要读 PDF、不要外查）。
2. 重写「## 一句话」下的那一段：**一个句子**（可以用冒号、破折号，但只能有一个句末标点），60-110 个汉字，必须同时包含——
   - 它做了什么（最关键的机制，一个短语）；
   - 它为什么重要 / 改变了什么格局（一个明确判断，来自正文的「在谱系中的位置」或「与 SB×RL 的关联」节）；
   - 一个最有说服力的数字或事实（来自正文「实验与证据」，若正文无数字则用定性结论）。
3. 禁止：「值得注意」「可以说」「一定程度上」等套话；平铺直叙的「本文提出了…方法，在…上取得了…」句式；正文里没有的数字、机构、比较对象。
4. 判断要有态度但有依据：正文写了「被超越」「理论位已占」「无真机证据」，一句话里可以直接用；正文没写的不能加。
5. 其余六节一个字都不要动。

## 写回与校验
- 用 Python 精确替换「## 一句话」与下一个「## 」之间的内容，UTF-8 写回，读回校验无 `??`。
- 在 `reports/ONELINER_LOG.md` 里每篇记一行：`<dir> | 旧一句话 → 新一句话`。
- 完成后输出一行：重写 N 篇。

## 待处理
所有 `papers/*/README.md`（`ls papers/` 列出的 51 个目录，全部处理）。
